---
"eve": patch
---

Add an authenticated manual session-compaction command for parked conversation sessions. The command compacts durable history without creating a model turn and reports its accepted, completed, or failed state through the session event stream.
